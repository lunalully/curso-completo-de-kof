// Solução 04 — array com quadrados de 1..10 e soma
main() {
    var arr = new Int[10]
    for (var i = 1; i <= 10; i = i + 1) {
        arr[i - 1] = i * i
    }
    var soma = 0
    for (var v in arr) {
        soma = soma + v
    }
    println(soma)   // 385
}
